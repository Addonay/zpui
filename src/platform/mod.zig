pub const backend = @import("backend.zig");
pub const @"test" = @import("test.zig");

pub const Platform = backend.Platform;
pub const CursorStyle = backend.CursorStyle;
pub const WindowHandle = backend.WindowHandle;
pub const WindowOptions = backend.WindowOptions;
pub const invalid_window_handle = backend.invalid_window_handle;
